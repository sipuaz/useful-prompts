# Useful Prompts

This is a collection that holds prompts that I have found useful while working with them.

## Directory Structure
```
.
├── chain-of-thoughts
│   └── init.xml // Gives instructions to an agent to setup a CoT, XML format, it may be useful to avoid ambiguity of characters inside the prompt
│   └── init.md // Gives instructions to an agent to setup a CoT, Markdown format
└── README.md
```